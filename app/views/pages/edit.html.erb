<%= error_messages_for :page %>

<% form_for(@page) do |f| %>
  <%= render 'form', :f => f %>
  <p>
    <%= f.submit "Update", :id => nil %>&nbsp;
    <%= button_link_to "Cancel and View", @page %>
  </p>
<% end %>
